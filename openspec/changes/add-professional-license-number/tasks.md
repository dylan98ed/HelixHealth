## 1. Model and migration

- [x] 1.1 Add nullable `Professional.license_number` with a 50-character limit and a named NULL-or-nonblank check after the current migration leaf. Preserve every existing Patient/Professional field. Verify model tests cover unknown and entered licenses, database rejection of empty/whitespace-only values, preserved `MN 001234` text, and independent generated registration numbers; run `uv run python manage.py makemigrations --check --dry-run` after generating the migration.
- [x] 1.2 Add a PostgreSQL migration regression for active/inactive, complete/incomplete profiles with user links, timestamps, generated numbers, and admissions. Verify forward migration sets only the new license to NULL, preserves all original values/relationships, and reverses/reapplies on an empty disposable database; run the focused migration tests.

## 2. Registration, maintenance, and representations

- [x] 2.1 Implement shared license normalization/validation and extend registration/completion services with required `license_number`; extend the update allowlist for license corrections. Update existing service callers and unrelated fixtures with explicit synthetic licenses. Verify missing/invalid registration licenses fail before number allocation or profile/group writes, valid prefixes/zeroes persist, omission on update preserves the current value, explicit blank cannot clear a license, and identifiers/status/history remain unchanged.
- [x] 2.2 Add License number to new/completion and edit forms, bound field errors, saved detail, and professional list/search results. Preserve both internal registration labels and all existing form fields. Verify unknown legacy values display Not recorded, untouched unknown edit controls omit the update, known-license clearing is rejected, normal POST and HTMX success/error paths work, and completed legacy profiles retain their existing eligibility/state.
- [x] 2.3 Integrate the additive license contract with professional JSON endpoints that exist at implementation time. If present, test required registration input, mutable update input, text/null detail/search output, unchanged existing response keys, and role/CSRF enforcement at HTTP boundaries. If absent, update the pending API contract/tasks in `implement-epics-1-and-2` to require these exact fields and record that no API runtime claim is made; verify the revised OpenSpec artifacts validate. Do not build unrelated pending API functionality as a prerequisite for the model/UI addition.

## 3. Acceptance and documentation

- [x] 3.1 Extend isolated browser journeys from signed-out `/` with a non-staff administrator and an account created through supported operator provisioning, initially without profile/medical role. Exercise missing-license feedback, successful `MN 123456` registration, distinct internal number, saved detail, DNI search, fresh clinical login, and license correction. Verify PostgreSQL persistence and unchanged account permissions/identity; include ordinary POST without JavaScript.
- [x] 3.2 Add legacy active/inactive completion/edit browser scenarios and corresponding disposable Compose journeys. Begin without the target license, preserve old admissions and completion state, add the license through visible controls, and verify retained IDs, active flags, and all previously supported patient/professional fields. Update acceptance persistence verification and operator documentation; never prepopulate the license being established by the journey.
- [x] 3.3 Reconcile affected pending field lists and examples in `implement-epics-1-and-2` with this additive capability, preserving existing fields, generated-number semantics, and historical completed task evidence. Document License number versus Registration number, required new input, and legacy Not recorded/Edit behavior. Verify both changes with `openspec validate --strict` and confirm no patient field or clinical-eligibility requirement was removed.
- [x] 3.4 Run appropriate PostgreSQL tests, Django checks, migration drift, Ruff, and mypy, then `uv run python .agents/skills/validate-live-app/scripts/validate_live_app.py`. Verify zero skipped browser scenarios, successful isolated and disposable Compose journeys, and persisted outcomes. Record entry points, personas, initial states, visible actions, final destinations, test counts, and the two validation environments before declaring implementation complete.
